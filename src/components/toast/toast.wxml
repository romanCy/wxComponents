<!--components/toast/toast.wxml-->

<view class="wxapp-toast-content-box" wx:if="{{toastData.isShow}}">
    <view class="wxapp-toast-content">
        <view class="wxapp-toast-content-text">
            {{toastData.msg}}
        </view>
    </view>
</view>
